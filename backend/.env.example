# SERVER-ONLY — available to the Express service at runtime, not shipped to the browser.
# On Vercel (Services) these live in the same project env UI but are NOT exposed to the Vite build.
#
# Local Docker Postgres (see docker-compose.yml). On Vercel use Neon/Supabase/Vercel Postgres.
DATABASE_URL=postgresql://zaotrak:zaotrak@localhost:5432/zaotrak
PORT=3001

# Stellar testnet
ESCROW_CONTRACT_ID=CC44LWA7OK6BIOHWZPZ2ES22UT4POT6KHDYU22FCVY3IXGGTGBXFBFP5
PRIZE_TOKEN_CONTRACT=CDLZFC3SYJYDZT7K67VZ75HPJVIEUVNIXF47ZG2FB2RMQQVU2HHGCYSC
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Optional — richer step-2 suggestions in Create Competition (falls back to templates)
# Groq is checked first if set (OpenAI-compatible API):
# GROQ_API_KEY=
# GROQ_MODEL=llama-3.3-70b-versatile
# Or OpenAI:
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
