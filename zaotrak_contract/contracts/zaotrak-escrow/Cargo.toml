[package]
name = "zaotrak-escrow"
version = "0.1.0"
edition = "2021"
publish = false
description = "ZaoTrak competition prize escrow on Soroban"

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
